% echo -n || Here is a demo with embedded comments using echo -n || ..
%
%
% cat GameBehaviorDemo | sed -n 's/^[>$]//p' > testactions
% emacs -batch -l dunnet < testactions > testout
End of file during parsing: Error reading from stdin
%
% less testout
% echo -n || From my having looked at what the above above less command
% echo -n || temporarily displayed, I saw that, at the end of testout,
% echo -n || there was an error, because this approach can't handle the
% echo -n || behavior of logginb in to pokey.
%
% echo -n || So, we stop the demo earlier:
%
% cat GameBehaviorDemo | sed -n 's/>type/>quit/;s/^[>$]//p' > testactions
%
% emacs -batch -l dunnet < testactions > testout
%
% less testout
% echo -n || From my looking at testout, I can see that it worked.
% echo -n || We can look at some of it together:
%
% tail -14 testout
>As you put the CPU board in the computer, it immediately springs to life.
The lights start flashing, and the fans seem to startup.
>I do not see that here.
>Old Building hallway
>Computer room
The panel lights are flashing in a seemingly organized pattern.
>You currently have:
A bracelet
A lamp
A shovel
A brass key
>I don't see that here.
>
You have scored 0 out of a possible 90 points.
%
%
% echo -n || Yes, it shows the ">", but not the command you piped-in.
% echo -n || It also shows the correct output.
%
% echo -n || So you can generate your own output and run: diff youroutput testout
% echo -n || There should be no differences.
%
